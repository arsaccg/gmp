%h2{'style' => 'text-align:center'} Items de Ordenes de Compra
%table#modal-more-items.table.table-striped.table-bordered.table-hover.has-tickbox.smart-form
  %thead
    %tr
      %th O/Compra
      %th Cód.Artículo
      %th Artículo
      %th Cantidad
      %th Pendiente
      %th Sector
      %th Fase
      %th C/Atención
      %th Seleccionar
  %tbody
    - @tableItems.each do |x|
      - @pending = x.amount.to_i - PurchaseOrderDetail.get_total_received(x.id).to_i
      %tr
        %td.col-md-1
          = x.id.to_s.rjust(5, '0')
        %td.col-md-1
          = x.delivery_order_detail.article.code
        %td.col-md-2
          = x.delivery_order_detail.article.name
        %td.col-md-1
          = x.amount
        %td.col-md-1
          = @pending
        %td.col-md-1
          = x.delivery_order_detail.sector.name
        %td.col-md-1
          = x.delivery_order_detail.phase.name
        %td.col-md-1
          = x.delivery_order_detail.center_of_attention.name
        %td.col-md-1
          %label.checkbox
            = check_box("order_delivery", "approved", { class: 'order_check' }, x.id)
            %i

%br

%button.btn.btn-sm.btn-primary.btn-prev#add-purchase-order-items{"type" => "button", "onclick" => "javascript:add_more_items();"}
  %i.fa.fa-plus
  Agregar

%button.btn.btn-sm.btn-primary.btn-prev{"type" => "button", "data-dismiss" => "modal"}
  %i.fa.fa-times
  Salir