#wid-id-0.jarviswidget{"data-widget-colorbutton" => "false", "data-widget-editbutton" => "false"}
  %header
    %span.widget-icon
      %i.fa.fa-eye
    %h2 Normas Técnicas
  .widget-body
    = simple_form_for([:libraries, @soft], html: {class: 'form-horizontal smart-form', autocomplete: 'off' }) do |f|
      %fieldset
        .row
          %section.col.col-md-8
            %label.label Nombre
            %label.input{for: "name"}
              %i.icon-append.fa.fa-user
              = f.input :name, placeholder: "Nombre", input_html: { class: 'form-control' }, label: false
        %br
        .row
          %section.col.col-md-12
            %label.label Descripción 
            %label.input
              = f.input :description, placeholder: "Descripción", input_html: { class: 'form-control' }, label: false, rows: 4
        %br
        .row
          %section.col.col-md-12
            %label.label Archivo
            -if @action == nil
              = f.input :file, :input_html => { :class => 'btn btn-default' }, label: false, as: "file", required: true
            -else
              = f.input :file, :input_html => { :class => 'btn btn-default' }, label: false, as: "file"
            %p.help-block
              - if @action == 'edit' && @soft.file.exists?
                = 'Archivo subido: ' + File.basename(@soft.file.path, File.extname(@soft.file.path))
              - else
                Subir el archivo correspondiente
        %br
        .row
          %section.col.col-md-12
            %label Tipo de Centro de Costo       
            %select.select2.form-control#speciality_select{:name =>"download_software[type_of_cost_center]"}
              %option{:value => nil, :disabled=>true, :selected => true} Seleccione una especialidad
              %option{:value => "saneamiento", :selected => "#{@soft.type_of_cost_center}"=="saneamiento"}
                Saneamiento
              %option{:value => "electrico", :selected => "#{@soft.type_of_cost_center}"=="electrico"}
                Eléctrico
              %option{:value => "civil", :selected => "#{@soft.type_of_cost_center}"=="civil"}
                Civil
              %option{:value => "administracion", :selected => "#{@soft.type_of_cost_center}"=="administracion"}
                Administración                         

      %footer
        %button.btn.btn-primary{type: "submit", :onclick => "part_block();"}
          %i.fa.fa-save 
          Guardar

:javascript
  $(document).ready(function () {

    if($('form[id^="edit_"]').length > 0) {
      submit_validate($('form[id^="edit_"]'));
    }else{
      submit_validate($('#new_download_software'));
    }

  });
  
  function submit_validate(form){
    $(form['selector']).ajaxForm({
      beforeSubmit: function() {
        $(form['selector']).validate({
          // Rules for form validation
          rules : {
            'download_software[name]' : {
              required : true
            },
            'download_software[description]' : {
              required : true,
            }
          },

          // Messages for form validation
          messages : {
            'download_software[name]' : {
              required : "Por favor, ingrese el nombre del documento"
            },
            'download_software[description]' : {
              required : "Por favor, ingrese una descripción para el documento"
            }
          },

          // Do not change code below
          errorPlacement : function(error, element) {
            error.insertAfter(element.parent());
            $('#modalLoadingLabelading').modal('hide');
            $('.modal-backdrop.fade.in').remove(); 
          }
        });
        // Remove all Help-inLine
        $(".help-inline").remove();
        // Client Valid
        return $(form['selector']).valid();
      },
      target: '#content',
      success: function (data){
        $(".help-inline").parent().addClass("state-error");
        $('#modalLoadingLabelading').modal('hide');
        $('.modal-backdrop.fade.in').remove(); 
      },
      error: function(xhr, status, error) {
        $('#modalLoadingLabelading').modal('hide');
        $('.modal-backdrop.fade.in').remove(); 
      }
    });
  }